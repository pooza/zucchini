<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="utf-8">
<title><%= @package[:full_name] %></title>
<style>
ul li {
  display: inline-block;
  float: left;
  width: 300px;
  height: 400px;
  font-size: 12px;
  margin: 5px;
  background-color: #ccc;
}
form {
  text-align: center;
}
</style>
</head>
<body>
  <form>
    <input type="search" name="q" value="<%= h @params['q'] %>">
    <input type="submit" value="検索">
  </form>
  <section>
    <ul>
      <% @movies.each do |movie| %>
        <li>
          <img src="<%= h movie[:thumbnail_href] %>"><br>
          <a href="<%= movie[:href] %>"><%= h movie[:name] %></a><br>
          path: <%= h movie[:href] %><br>
          size: <%= h movie[:binary_size] %> (<%= h movie[:size].commaize %>b)<br>
          width: <%= h movie[:width].commaize %>px<br>
          height: <%= h movie[:height].commaize %>px<br>
          duration: <%= h '%0.2fsec.'%[movie[:duration]] %><br>
        </li>
      <% end %>
    </ul>
  </section>
</body>
</html>